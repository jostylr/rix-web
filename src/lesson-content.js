// Curriculum source for generated tutorial pages. Markdown-authored lessons in
// tutorials/ take precedence; this catalog keeps the rest equally substantial.
export const lessonContent = {
  "2d": ["Tuples are fixed positional groups; strings are ordinary values with indexes and methods. Use a tuple when position has meaning and a map when a key has meaning.", "pair := ( 3, 5 )\npair[2]", "Compare a tuple with a two-entry map, then index a string from the end.", "Create a tuple of three exact measurements and return its last value."],
  "2e": ["Tensors describe rectangular data. The shape comes after `{:` and semicolons separate rows; tensor locators are one-based tuples.", "grid := {:2x2: 1, 2; 3, 4 }\ngrid[2, 1]", "Try a slice such as `grid[1:2, 2]`; tensor views preserve their shape.", "Create a 2x2 identity tensor and retrieve its lower-right entry."],
  "3": ["RiX expressions stay exact by default. Arithmetic, intervals, collections, and calls all compose into values you can bind, inspect, and transform.", "third := 1 / 3\nthird + third + third", "Use the detail pages to learn which symbols build an expression and how missing values behave.", "Write an expression that combines a fraction, an interval, and a comparison."],
  "3a": ["Precedence follows the usual mathematical order, while adjacent terms can mean multiplication or callable application. Parentheses are the clearest way to state a grouping.", "2 + 3 * 4 ^ 2", "Try `3(2 + 1)` for implicit multiplication and define an uppercase function to see adjacent application.", "Define `Double(x)` and compare `Double 3 + 1` with `Double(3 + 1)`."],
  "3b": ["RiX has exact syntaxes for forms that are often approximate elsewhere: repeating decimals, mixed numbers, continued fractions, and alternate bases.", "a := 0.#3\nb := 1..2/3\na + b", "A repeating marker describes a rational pattern, not a floating-point approximation.", "Evaluate `3.~7~15~1` and compare it with `355 / 113`."],
  "3c": ["An interval keeps both endpoints in a single value, so uncertainty travels with the calculation. Interval arithmetic returns bounds rather than silently selecting one point.", "a := 2:3\nb := 4:5\na * b", "Use a three-part colon expression to ask whether a value lies between two bounds.", "Test whether 7 lies between 2 and 10, then test a value outside the interval."],
  "3d": ["A hole means a missing value; it is distinct from the explicit null value `_`. Hole coalescing gives a fallback without treating every false-like value as absent.", "values := [1]\nvalues[2] ?| 99", "Holes are useful in partial collections, optional call arguments, and soft matching.", "Use `?|` to supply a default for a missing map or array lookup."],
  "4": ["A RiX binding names a cell. That makes aliasing and mutation explicit rather than accidental, and destructuring applies the same assignment choices to structured data.", "x := 5\ny = x\nx += 1\ny", "The next pages separate cell identity, patterns, and metadata so each rule stays visible.", "Create one alias and one fresh copy of the same value, then update the original."],
  "4a": ["Use `=` to alias a cell, `:=` to create a fresh copy, and `~=` to replace a cell's value in place. `===` asks whether two names still share a cell.", "x := 5\ny = x\nx ~= 9\nx === y", "Deep-copy forms (`::=` and `~~=`) matter when nested collections must not share children.", "Bind `a`, alias it as `b`, copy it as `c`, update `a`, and inspect both names."],
  "4b": ["Destructuring reads a structured source once and binds selected pieces outward. Arrays, tuples, maps, and tensors can all be patterns.", "[first, second, ...rest] := [1, 2, 3, 4]\nrest", "A map pattern can rename a source key, and missing simple entries become holes.", "Destructure a map with keys `x` and `y` into two differently named variables."],
  "4c": ["Indexes select stored values; metadata and methods describe how a value behaves. Receiver-first method syntax keeps the subject of an operation visible.", "values := [1, 2]\nvalues.Push(3)", "Use `.key` for stable object identity and reserve bang methods for deliberately mutable operations.", "Create an array, make a pushed copy, and confirm the original remains unchanged."],
  "5": ["Uppercase names are callable. RiX supports ordinary functions, lambdas, multifunctions, lexical scope, and partial application without leaving expression syntax.", "Square(x) -> x ^ 2\nSquare(12)", "The detail pages explain dispatch and closure behavior before using those features in larger programs.", "Define a function that returns the area of a rectangle."],
  "5a": ["A function definition names parameters and an expression body. Lambdas use the same arrow form without a name; rest parameters and spreads handle variable arity.", "Add(x, y) -> x + y\nAdd(3, 4)", "Lowercase names are values, while uppercase names can consume adjacent arguments as callable application.", "Define `Scale(value, factor)` and call it with two exact fractions."],
  "5b": ["A multifunction holds ordered variants. Preparation clauses decide whether a variant accepts an input; `$` refers to the active variant and `$$` to the parent multifunction.", "Abs(x) ?- [x >= 0] -> x\nAbs(x) -> -x\nAbs(-7)", "Use a soft prep clause to skip a variant and a strict one when failure should stop evaluation.", "Create a two-variant function that treats zero differently from other values."],
  "5c": ["RiX resolves names lexically. Inside a nested callable, `@name` accesses an outer binding, while block import headers make copying and aliasing explicit.", "x := 10\nReadOuter() -> @x\nReadOuter()", "Closures retain the scopes they need; updates should state whether they target the local or outer cell.", "Define a function that reads an outer value and combines it with its argument."],
  "5d": ["Partial functions leave a callable shape behind. Operator aliases and numbered placeholders make small callbacks readable, especially inside collection pipelines.", "Double := @*(_1, 2)\nDouble(9)", "Arity caps control how much callback context a partial accepts when a pipe supplies locator and source values.", "Create a partial that subtracts its input from 10."],
  "6": ["Control in RiX remains expression-oriented. Ternaries choose values, brace sigils organize blocks and containers, and deferred forms postpone work.", "x := -4\nx > 0 ?? x ?: -x", "Use the detail pages before relying on loop headers or caller-scope dynamic evaluation.", "Write a ternary that labels a value as positive or non-positive."],
  "6a": ["The `??` and `?:` pair reads as condition, true branch, false branch. Each branch can be an expression or a code block.", "x := 7\nx % 2 == 0 ?? \"even\" ?: \"odd\"", "Case containers are useful when several guarded alternatives need to stay together.", "Return the larger of two values with a ternary."],
  "6b": ["Braces create blocks and sigilled containers: `{=`, `{|`, `{:`, `{;`, `{@`, and others. Spaces after a sigil are significant in RiX syntax.", "{; x := 2; y := 3; x * y }", "Loop headers separate initialization, condition, body, update, and after slots with semicolons.", "Make a block that binds two values and returns their product."],
  "6c": ["Deferred values keep code for later. Dynamic evaluation and `@@` can intentionally use the caller's scope, so they should be introduced only when ordinary functions are not enough.", "{; value := 3; value + 1 }", "RatCalc can evaluate ordinary blocks; script-level dynamic evaluation is best explored after learning scope.", "Write a block whose final expression uses a name bound earlier in the block."],
  "7": ["Pipes transform a value through a readable series of operations. Generators create sequences, while regexes and strings add pattern-based transformation.", "[1, 2, 3] |>> (x) -> x ^ 2", "Details cover callback locator/source parameters and the difference between mapping, filtering, and reducing.", "Map an array of three values to their doubles."],
  "7a": ["A plain pipe feeds a value into a callable. Collection pipes map, filter, reduce, reverse, sort, and quantify while preserving the collection shape when possible.", "[1, 2, 3] |>> (x) -> x * 2", "Callback parameters can receive the value, its locator, and its source collection.", "Filter an array to retain values greater than 2."],
  "7b": ["Generator syntax creates a sequence from a compact pattern, then composes naturally with filters and pipes. Keep a stopping condition or limit visible.", "[1 |+ 2 |^ 5]", "Generated values are a good fit for exact progressions and small mathematical investigations.", "Create a generator whose values grow by three, then inspect its first few terms."],
  "7c": ["Regex literals support matching modes, while strings support indexes, slices, and methods. Keep matching and replacement logic as small testable expressions.", "text := \"RatCalc\"\ntext[1:3]", "Regex match objects can be passed through a pipeline when a pattern needs more than a yes/no answer.", "Create a string and retrieve its final character with a negative index."],
  "8": ["Semantic information lets a value carry a type, traits, and unit-like meaning without erasing its exact representation. Conversions make the requested change explicit.", "value := 7\nvalue ? :integer", "The details distinguish runtime facts from sticky semantic metadata.", "Ask whether an exact fraction satisfies the rational semantic type."],
  "8a": ["Use `? :name` to inquire about semantic membership, `~:` for a soft conversion, and `~!:` for a strict conversion that reports failure.", "value := 7\nvalue ? :integer", "A conversion does not mutate a binding unless you assign the converted result back.", "Try a type inquiry on an integer and on a fraction."],
  "8b": ["Headers attach names, types, traits, and capture modes to constructed values. Traits describe protocols; types can supply conversion and method behavior.", "{^ /#answer/ 42}", "Sticky semantic headers persist through suitable updates, while ephemeral runtime facts are rebuilt.", "Create a headered value with a semantic name and inspect it."],
  "8c": ["Units are separate from ordinary number parsing and semantic types. Scientific and algebraic unit notation should make the physical meaning of a value visible.", "distance := 3\ndistance", "Unit-heavy examples depend on the active unit capability; RatCalc keeps the core exact-number lesson runnable.", "Record a quantity and write a comment describing its intended unit before extending it in a unit-enabled host."],
  "9": ["The dot system object exposes deliberately granted capabilities. Assertions, symbolic specifications, diagnostics, and tests use that same explicit system boundary.", ".ADD(3, 4)", "The advanced pages show how to ask the runtime for more without making hidden global functions.", "Use a dot capability to compute an arithmetic result."],
  "9a": ["System calls use `.Name(args)` or an `@_` form. The system context can expose aliases while a sandbox withholds capabilities that a script should not receive.", ".MUL(6, 7)", "Use the reference to distinguish system functions from user-defined uppercase callables.", "Call `.ADD` and compare it with an ordinary `+` expression."],
  "9b": ["Assertions state conditions that must hold; symbolic specs preserve an expression structure for consumers such as polynomial tooling. Keep assertions small and diagnostic.", "3 < 5", "Symbolic specification and solver forms are advanced features; use them after functions and types feel familiar.", "Write a comparison that should pass, then change it to one that should fail and inspect the diagnostic."],
  "9c": ["Diagnostics turn warnings, stops, traces, and tests into structured values. Start with small test cases before adding tracing to a larger calculation.", ".Info(\"lesson\", 1)", "Some diagnostic capabilities are best viewed in a script or CLI host; RatCalc keeps their invocation explicit.", "Use `.Help(\"diagnostic\")` to inspect available related guidance."],
  "10": ["RiX scripts can declare interfaces, import other RiX files, and run inside capability frames. JavaScript modules and language extensions are host-level features with explicit trust boundaries.", "## This lesson discusses host-facing features\n1 + 1", "RatCalc intentionally does not execute arbitrary local JavaScript modules in the browser.", "Describe the inputs and outputs your first RiX script would need, then prototype its core expression here."],
  "10a": ["A RiX script can bind inputs, export outputs, and import another script through an explicit interface. Capability modifiers narrow what an imported script can do.", "## Script imports need a host-visible .rix file\n1 + 1", "Use the command-line runner for real file imports; RatCalc can still prototype the script body.", "Write a function that could be the body of a small reusable script."],
  "10b": ["JavaScript imports cross a host trust boundary. RiX can model module values and calls, but RatCalc deliberately previews rather than executes selected browser modules.", "## JavaScript module execution is intentionally disabled in RatCalc\n1 + 1", "This protects a calculator page from silently running code selected from a local file.", "Use a RiX map to sketch the data a future JavaScript module would return."],
  "10c": ["Language maintainers can use the system loader to register keywords, system functions, and custom operators. Pure RiX users receive only the configured surface.", "## Extension registration belongs to a trusted host\n1 + 1", "Study the parser and three-tier system documentation before adding syntax or capabilities.", "Write a short note describing a domain operation you would expose as a system capability."],
};

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Normalized semantic record before mathematical JSON tagging; RiX map keys are case-insensitive. Exact values and expression graphs use the shared mathematical JSON codec. Shape validation does not verify a mathematical claim: invoke its bounded runtime replay checker. See documentation/eval/course-calculus.md.",
  "type": "object",
  "$id": "https://rix.ratmath.com/schema/calculus-derivative-declaration/v1.schema.json",
  "title": "calculus-derivative-declaration v1",
  "required": [
    "schema",
    "functionid",
    "derivativeid",
    "order",
    "domain",
    "range",
    "options",
    "evidencelevel",
    "certified",
    "execution"
  ],
  "properties": {
    "schema": {
      "const": "rix.calculus.derivative-declaration@1"
    },
    "functionid": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "derivativeid": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "order": {
      "description": "Exact semantic value; runtime checks types, limits, domains and evidence."
    },
    "domain": {
      "description": "Exact semantic value; runtime checks types, limits, domains and evidence."
    },
    "range": {
      "description": "Exact semantic value; runtime checks types, limits, domains and evidence."
    },
    "options": {
      "description": "Exact semantic value; runtime checks types, limits, domains and evidence."
    },
    "evidencelevel": {
      "const": "declaredByCaller"
    },
    "certified": {
      "type": "null"
    },
    "execution": {
      "const": "inert"
    }
  },
  "additionalProperties": true
}
